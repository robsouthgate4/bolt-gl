export default class PointCloudLoader {
    private points;
    private path;
    load(path: string): Promise<void>;
    private loadData;
    private loadCompressedBinary;
    private parseBinary;
}
