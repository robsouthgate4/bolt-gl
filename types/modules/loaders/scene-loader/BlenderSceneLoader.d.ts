export default class BlenderSceneLoader {
    private objects;
    private materials;
    private path;
    load(path: string): Promise<void>;
    private loadScene;
    private loadCompressedBinary;
    private parseBinary;
}
